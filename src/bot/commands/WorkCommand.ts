
/**
 * The work command message that gets sent whenever a user works. The following gets replaced:
 * * `{money}` - The amount of money made from working
 */
export type WorkCommandMessage = string;

export default class WorkCommand {
    //
}